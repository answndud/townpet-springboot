# AI Slop·Dead Code·Dependency 정리 감사

## 실행 정보

- commit: `7b1a88b`
- 방법: 미참조 파일 스캔, import 그래프 대조, Gradle dependencyInsight, pnpm lockfile 실측 후 삭제 전 호출부 확인

## 발견·조치 내역

### 제거 — 죽은 코드 (2개 파일)

| 파일 | 확인 근거 |
|---|---|
| `AdoptionPage.tsx` | import 0건, 라우트 없음. `AdoptionCreatePage`/`AdoptionDetailPage`가 실제 사용 중 |
| `features/discovery/HomePopularSection.tsx` | import 0건, 동적 참조 없음 |

### 재분류 — 의존성

| 패키지 | 이전 | 이후 | 근거 |
|---|---|---|---|
| `vite` | dependencies | devDependencies | 빌드 도구이며 runtime bundle에 포함되지 않음 |
| `@vitejs/plugin-react` | dependencies | devDependencies | 동일 |

### 유지 판단 — 삭제 검토 후 보류

| 항목 | 판단 근거 |
|---|---|
| `bouncycastle:bcprov-jdk18on` constraint | 직접 import는 없으나 minio의 transitive 의존(1.81 → 1.81.1 pin). CVE 대응 목적으로 유지 |
| `LegacyHealthController` (`/api/health`) | parity matrix와 SecurityConfig permitList에서 참조. legacy 호환 계약 |
| `measure-performance.mjs`, `measure-browser-performance.mjs` | package.json script로 실행 가능한 운영 측정 도구 |

### 스캔 결과 — 문제 없음 확인

- 주석 처리된 구현 코드: 없음
- TODO/FIXME/HACK: 없음
- 중복 유틸리티·wrapper-around-wrapper: 발견되지 않음
- deprecated 프론트엔드 패키지: 없음 (runtime deps 3개로 최소 유지)

## 회귀 검증

| 검증 | 결과 |
|---|---|
| `pnpm typecheck` / 37 tests / `pnpm build` | 통과 (bundle budget 이내) |
| `./gradlew check` (전체) | 통과 |
| GHCR 발행 + netcup 배포 [run 32556817196] | success |
| 운영 `/actuator/health` | 200 |

## 효과

runtime dependencies를 5개→3개로 축소해 production 설치 표면을 줄였고, 죽은 코드 94줄을 제거했다. 동작 변화는 없으며 번들 크기·테스트 결과 모두 기존 기준선을 유지한다.
