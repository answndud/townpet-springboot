# 운영 검증 기록 — 2026-08-20

## 대상

- 환경: netcup VPS TownPet production
- 확인 대상: VPS health, SMTP TLS, DNS 인증, paired backup 복구
- Secret·토큰·복호화 키·개인정보는 기록하지 않는다.

## VPS

- TownPet backend, PostgreSQL, MinIO container health: PASS
- `/opt/townpet/secrets/portfolio.env` file mode `600`: PASS
- 현재 VPS image는 보안 개선 작업 이전 commit이므로 최신 로컬 보안 코드는 아직 배포 증거가 없다.

## SMTP/TLS/DNS

- VPS에서 `smtp.resend.com:2587` STARTTLS 연결 및 certificate verification: PASS
- SPF TXT lookup: provider record 확인 실패
- DMARC TXT lookup: provider record 확인 실패
- DKIM selector lookup: provider record 확인 실패
- 실제 verification/password-reset 수신함 도착: 수신 주소 미지정으로 미검증

판정: SMTP transport는 PASS지만 domain authentication과 end-to-end deliverability는 WARN이다. provider dashboard의 정확한 DKIM selector와 요구값을 확인한 뒤 DNS에 등록하고, 실제 수신함 smoke를 다시 실행해야 한다.

## Paired backup

- backup id: `20260819T162929Z`
- VPS에서 DB·MinIO paired backup 생성: PASS
- age encrypted archive의 VPS/local SHA-256 일치: PASS
- 로컬 보호 키를 통한 decrypt 및 `manifest.sha256` 검증: PASS
- disposable PostgreSQL/MinIO 환경 복구: PASS
- 복구 후 DB publication row: `50`
- 복구 후 upload asset row: `0`
- 복구 후 MinIO object: `0`
- DB/object count 대사: PASS

이번 backup은 media object가 0개인 운영 상태에서 생성되었으므로 non-empty media restore를 증명하지 않는다. RPO/RTO 수치와 failure webhook 수신은 아직 기록하지 않았다.

## 남은 운영 조치

1. Resend sender domain의 SPF/DKIM/DMARC를 provider 요구값으로 등록하고 verified 상태를 확인한다.
2. 실제 수신함으로 verification/password-reset을 보내 링크와 Outbox 상태를 확인한다.
3. 최신 `backup-portfolio.sh`, `encrypt-backup.sh`를 VPS에 배포하고 failure webhook을 의도적으로 재현해 수신을 확인한다.
4. 최신 보안 개선 commit을 포함한 image를 VPS에 배포하고 health·MFA·upload smoke를 재실행한다.
5. 최신 commit의 GitHub security workflow 실행 결과를 확인한다.
