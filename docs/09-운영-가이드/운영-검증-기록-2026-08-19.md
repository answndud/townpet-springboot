# 운영 검증 기록 — 2026-08-19

## 대상

- 환경: netcup VPS TownPet production
- 배포 commit: `fc046dd45d2f81136d94036934554627ed5f4767`
- 배포 workflow: `32217492831`

## SMTP

- 기존 `smtp.resend.com:587`은 VPS에서 연결되지 않았다.
- Resend가 지원하는 `2587` STARTTLS 포트로 VPS 환경을 변경했다.
- `smtp.resend.com:2587` TCP 연결과 STARTTLS handshake를 확인했다.
- 협상 결과는 TLS 1.3, `TLS_AES_256_GCM_SHA384`, peer certificate `*.resend.com`, verification `OK`였다.
- backend는 `SPRING_MAIL_PORT=2587`로 재기동 후 `healthy`였다.
- 실제 TownPet verification/password-reset 메일의 수신함 도착은 수신 주소가 지정되지 않아 아직 검증하지 않았다.

## MinIO browser/CORS

- media endpoint와 TownPet endpoint가 HTTPS HTTP 200을 반환했다.
- MinIO bucket anonymous policy는 `private`였다.
- CORS preflight가 `POST`, `PUT`, `GET`, `OPTIONS`와 `Content-Type`을 허용했다.
- 실제 media response의 `Access-Control-Allow-Origin`은 `https://townpet.cloud` 단일 값으로 반환됐다.
- 초기 검증에서 presigned POST 미허용 및 중복 origin header를 발견했고, Caddy media 설정과 배포 workflow의 Caddy 파일 동기화 경로를 수정한 뒤 재배포했다.
- 실제 로그인 owner의 presigned POST와 signed read expiry/ownership browser smoke는 별도 계정·파일 fixture가 필요해 남아 있다.

## Paired backup

- VPS에서 DB·MinIO paired backup `20260819T044404Z`를 생성했다.
- backup은 age로 암호화해 VPS와 분리된 `/Users/alex/.config/townpet/backups`에 복사했다.
- 로컬 보호 키로 복호화하고 `manifest.sha256` 검증을 통과했다.
- manifest의 `media_objects=0`을 확인했다. 현재 운영 MinIO에 백업 대상 object가 없으므로 object 복원은 빈 목록 상태다.
- 별도 restore 환경에서 현재 backup을 실제 DB·object로 복원하는 절차와 RPO/RTO 측정은 아직 남아 있다.

## 남은 외부 검증

1. 실제 수신함을 지정해 TownPet verification/password-reset 발송과 도착을 확인한다.
2. 로그인 owner의 presigned POST, signed read expiry, 다른 member/guest isolation을 browser에서 확인한다.
3. 현재 encrypted backup을 disposable PostgreSQL/MinIO에 복원하고 row/object 보존과 RPO/RTO를 기록한다.
4. `.env.deploy.local` 조회 중 Resend/GHCR credential이 도구 출력에 노출된 사고가 있었다. 해당 Resend API key와 GHCR token은 즉시 폐기·교체해야 하며, 교체 전 production release는 차단한다.

비밀번호, API key, SMTP credential, 백업 복호화 키는 이 문서에 기록하지 않는다.
