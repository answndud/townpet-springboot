import { FormEvent, useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ApiError, hospitalReviewApi, type HospitalReview } from "./api/client";
export default function HospitalReviewPage(){const navigate=useNavigate();const [items,setItems]=useState<HospitalReview[]>([]);const [query,setQuery]=useState("");const [error,setError]=useState<string|null>(null);const [form,setForm]=useState({hospitalName:"",address:"",rating:"5",body:""});useEffect(()=>{hospitalReviewApi.list(query).then(setItems).catch(()=>setError("병원 후기를 불러오지 못했습니다."));},[query]);async function submit(e:FormEvent){e.preventDefault();try{await hospitalReviewApi.create({...form,rating:Number(form.rating)});setForm({hospitalName:"",address:"",rating:"5",body:""});setError("후기를 등록했습니다.");hospitalReviewApi.list(query).then(setItems);}catch(x){if(x instanceof ApiError&&x.status===401)navigate("/login?next=/hospital-reviews");else setError(x instanceof ApiError&&x.status===409?"같은 병원에는 한 번만 후기를 작성할 수 있습니다.":"후기를 등록하지 못했습니다.");}}return <main className="page localcare-page"><section className="localcare-hero"><p className="eyebrow">HOSPITAL REVIEWS</p><h1>동물병원 후기</h1><p>병원명과 주소를 함께 확인하고 반려생활 경험을 나눠 보세요.</p></section><form className="surface-card localcare-search" onSubmit={e=>{e.preventDefault();setQuery(query.trim())}}><label>병원 검색<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="병원명을 입력해 주세요" /></label><button className="button button-soft" type="submit">검색</button></form>{error?<p role="alert">{error}</p>:null}<section className="localcare-grid" aria-label="병원 후기 목록">{items.map(item=><article className="surface-card localcare-card" key={item.id}><span className="publication-chip publication-chip-primary">{"★".repeat(item.rating)}</span><h2>{item.hospitalName}</h2><small>{item.address}</small><p>{item.body}</p></article>)}</section><form className="surface-card publication-fields" onSubmit={submit}><h2>후기 작성</h2><label>병원명<input required value={form.hospitalName} onChange={e=>setForm({...form,hospitalName:e.target.value})}/></label><label>주소<input required value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label>평점<select value={form.rating} onChange={e=>setForm({...form,rating:e.target.value})}>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select></label><label>후기<textarea required value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/></label><button className="button button-primary" type="submit">후기 등록</button></form><Link className="publication-text-link" to="/">홈으로</Link></main>}
